{
  "name": "youtube-sync",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "yarn workspace www build && mv ./packages/www/build ./packages/server/",
    "start": "yarn workspace server start"
  }
}
